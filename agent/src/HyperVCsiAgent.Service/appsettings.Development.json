{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Agent": {
    "//": "Dev-only default so the service starts without --config. Real deployments point --config at a file on the CSV; see agent.config.example.json.",
    "CsvVolumesRoot": "./dev-volumes"
  }
}
