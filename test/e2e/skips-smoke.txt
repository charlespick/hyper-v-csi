# Extra skips for the smoke profile, on top of skips.txt. Temporary by design:
# every line here is a test this driver is expected to pass eventually, held
# back only so the first integration runs fail on one simple thing at a time
# instead of on a cascade. The full profile runs all of them.
#
# Same format as skips.txt: one Ginkgo skip regex per line.

# Anything upstream tags as long-running, except multiVolume: the 2026-08-14
# full-profile run against a two-node cluster passed all 14 of its specs,
# including the 4 cross-node detach/attach cases, so it no longer needs
# holding back here. [Slow] itself can't stay the skip pattern, since
# multiVolume carries that tag too and Go's regexp has no negative lookahead
# to carve one suite back out of a tag match — these three are what's left of
# it, enumerated by suite name instead. Same trade as the [Feature:
# enumeration below: a new [Slow] suite upstream adds later runs instead of
# being silenced, which is the right way round. Raising the timeouts in
# testdriver.yaml is the answer to slow; this profile is about reducing what
# happens at once.
\] subPath .*\[Slow\]
\] volumeMode .*\[Slow\]
\] provisioning .*\[Slow\]

# Ginkgo runs these alone, and each is the load-piling kind: the volume-limits
# test provisions until the node refuses, and the ReadWriteOncePod preemption
# test evicts a running pod to take its volume.
\[Serial\]

# Writes and reads back files up to MaxFileSize (100 MiB) through the mount. A
# genuine data-integrity test, and the heaviest single I/O in the run.
volumeIO
